# Requirements Traceability Matrix — etd-airlines_1

**Run ID:** `2026-07-22T17-30-27Z`  
**Sources:** `06b_authoring_revised.md`, `02_discovery.md`, `src/test/java/com/airlines/etd/FlightServiceTest.java`

## Coverage summary

- Requirement IDs traced: **39**
- Directly evidenced requirements: **30**
- Governance/gap-traced requirements with explicit `no direct source` markers: **9**
- Requirement IDs linked to existing automated tests: **6** (via **2** existing test methods: `TS-001`, `TS-002`)
- Fully untraceable requirements: **0**

## Matrix

| Req ID | Confidence | Requirement | Discovery finding(s) | Source-code component(s) | Observed behavior | Business process / use case | Upstream business objective / requirement | Linked FR / control trace | Acceptance criterion / approval gate | Existing test trace | Trace status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| FR-001 | Confirmed | Initialize the dashboard and retrieve the current flight list and statistics on application load. | `02_discovery.md:39, 87-89` | `app.js:9-16`; `dashboard.js:24-35` | App start initializes the dashboard, fetches flights/statistics, and begins live-board rendering. | `UC-001` Monitor Current Departures | `BO-001` Provide a live departure board | Self | Initial load retrieves flights + statistics before first board render and supports the UC-001 postcondition. | No existing automated test identified (`06b_authoring_revised.md:943-947`). | Direct source + no automated test |
| FR-002 | Confirmed | Display KPI values for total flights, on-time flights, delayed flights, cancelled flights, on-time percentage, and average delay. | `02_discovery.md:32, 88-89` | `index.html:57-93`; `dashboard.js:37-48` | Dashboard KPI cards render the six current operational counters/rates. | `UC-001` Monitor Current Departures | `BO-001` Provide a live departure board | Self | All six KPI values are visible and refresh with the current flight dataset. | No existing automated test identified (`06b_authoring_revised.md:943-947`). | Direct source + no automated test |
| FR-003 | Confirmed | Display each flight with operational timing, route, gate, status, delay reason, and row-level actions. | `02_discovery.md:34-35, 88` | `index.html:133-160`; `dashboard.js:84-129` | Board rows show route/timing/status context plus Update ETD, Mark Departed, Cancel, and Delete actions. | `UC-001` Monitor Current Departures | `BO-001` Provide a live departure board | Self | Each rendered row shows the required fields and row actions for the loaded flight set. | No existing automated test identified (`06b_authoring_revised.md:943-947`). | Direct source + no automated test |
| FR-004 | Confirmed | Display a live-status badge and last-update timestamp for the current board refresh. | `02_discovery.md:44, 88-89` | `index.html:13-25`; `dashboard.js:203-206` | Header shows live monitoring context and stamps the latest refresh time. | `UC-001` Monitor Current Departures | `BO-001` Provide a live departure board | Self | The active board visibly shows live status and the timestamp from the last successful refresh. | No existing automated test identified (`06b_authoring_revised.md:943-947`). | Direct source + no automated test |
| FR-005 | Confirmed | Execute free-text search against flight number, airline, origin, and destination using the flights endpoint. | `02_discovery.md:33, 45-46, 91-94, 75` | `dashboard.js:24-30`; `FlightController.java:29-35`; `FlightService.java:109-123` | Dashboard issues `GET /api/flights?search=` and the backend performs case-insensitive substring matching across four fields. | `UC-002` Search, Filter, and Prioritize Flights | `BO-002` Enable quick flight prioritization | Self | Search returns only flights whose number, airline, origin, or destination matches the query. | No existing automated test identified (`06b_authoring_revised.md:943-947`). | Direct source + no automated test |
| FR-006 | Confirmed | Allow the loaded flight list to be filtered by status without changing stored data. | `02_discovery.md:33, 46, 94` | `dashboard.js:50-58` | Status filtering is applied client-side after result loading. | `UC-002` Search, Filter, and Prioritize Flights | `BO-002` Enable quick flight prioritization | Self | Changing the status filter narrows the visible list only; backend data is not mutated. | No existing automated test identified (`06b_authoring_revised.md:943-947`). | Direct source + no automated test |
| FR-007 | Confirmed | Allow the loaded flight list to be sorted by ETD, scheduled departure, largest delay, or flight number. | `02_discovery.md:33, 46, 94` | `dashboard.js:59-72` | Loaded results can be reordered by four supported sort choices. | `UC-002` Search, Filter, and Prioritize Flights | `BO-002` Enable quick flight prioritization | Self | The UI supports all four declared sort options against the loaded list. | No existing automated test identified (`06b_authoring_revised.md:943-947`). | Direct source + no automated test |
| FR-008 | Confirmed | Capture flight number, airline, aircraft type, origin, destination, gate, and scheduled departure on the Add Flight form. | `02_discovery.md:37, 48, 97-100` | `index.html:193-245`; `addFlight.js:18-27` | Add Flight UI exposes the seven required input fields and submits them to `POST /api/flights`. | `UC-003` Register a New Flight for Monitoring | `BO-003` Add and maintain operational flight records | Self | The Add Flight tab provides all required inputs and submits a create-flight payload. | `TS-001` / `createFlightAssignsIdAndDefaults` (`FlightServiceTest.java:30-47`) partially covers create-flight baseline behavior. | Direct source + automated test link |
| FR-009 | Confirmed | On successful create, set ETD equal to scheduled departure, assign status `SCHEDULED`, confirm success in the UI, and refresh the dashboard. | `02_discovery.md:99-100, 55, 136` | `FlightService.java:52-63`; `addFlight.js:32-41` | Server defaults ETD/status during creation; UI shows success feedback and refreshes the board. | `UC-003` Register a New Flight for Monitoring | `BO-003` Add and maintain operational flight records | Self | A valid create request stores a scheduled flight with ETD=STD and immediately confirms success to the operator. | `TS-001` / `createFlightAssignsIdAndDefaults` (`FlightServiceTest.java:30-47`) covers ID/default ETD behavior; UI refresh remains untested. | Direct source + partial automated test |
| FR-010 | Confirmed | Present an ETD update modal containing flight context and fields for new ETD, optional delay reason, and optional delay notes. | `02_discovery.md:49, 103-104` | `dashboard.js:166-176`; `index.html:250-299` | Operators open a modal from the board and edit ETD, reason, and notes with flight context visible. | `UC-004` Update ETD and Record Delay Context | `BO-004` Capture disruption context | Self | The ETD workflow exposes the modal, contextual labels, and the required/optional fields before submission. | `TS-002` / `updateEtdMarksFlightAsDelayed` (`FlightServiceTest.java:49-69`) covers update semantics but not modal presentation. | Direct source + partial automated test |
| FR-011 | Confirmed | After a valid ETD update, save the revised ETD and immediately recalculate the flight status. | `02_discovery.md:104-105, 137-144` | `FlightService.java:65-86`; `EtdCalculationService.java:20-41` | A valid ETD change persists the new estimate, recomputes delay/state, and returns the updated flight. | `UC-004` Update ETD and Record Delay Context | `BO-004` Capture disruption context | Self | A valid ETD revision updates stored ETD and recomputes status from current delay/timing rules in the same flow. | `TS-002` / `updateEtdMarksFlightAsDelayed` (`FlightServiceTest.java:49-69`) covers recalculated delay status for a 45-minute slip. | Direct source + automated test link |
| FR-012 | Confirmed | Allow optional cancellation notes, set the flight status to `CANCELLED`, store the notes on the flight record, and refresh the board. | `02_discovery.md:107-109, 146` | `dashboard.js:147-152`; `FlightService.java:88-93` | Cancellation uses a prompt for notes and persists the outcome as `CANCELLED` plus `delayNotes`. | `UC-005` Cancel a Flight | `BO-004` Capture disruption context | Self | Cancellation succeeds with optional notes, stores them, and moves the flight to terminal cancelled state. | No existing automated test identified for cancellation (`06b_authoring_revised.md:945-946`). | Direct source + no automated test |
| FR-013 | Confirmed | Require confirmation for manual departure, set status to `DEPARTED`, record actual departure time, and refresh the board. | `02_discovery.md:111-113` | `dashboard.js:142-146`; `FlightService.java:95-100` | Manual departure flow confirms the action and stores an ATD timestamp with `DEPARTED` status. | `UC-006` Mark a Flight Departed | `BO-003` Maintain operational flight records | Self | A confirmed depart action records `actualDeparture`, sets `DEPARTED`, and refreshes the visible board. | No existing automated test identified for manual departure (`06b_authoring_revised.md:946`). | Direct source + no automated test |
| FR-014 | Confirmed | Automatically move a flight to `DEPARTED` when current time is past ETD during status recomputation. | `02_discovery.md:114, 141-144` | `EtdCalculationService.java:27-33`; `FlightSimulationService.java:96-109` | Background status recomputation transitions past-ETD flights to `DEPARTED` without manual action. | `UC-006` Mark a Flight Departed | `BO-001` Keep the board current; `BO-003` maintain flight records | Self | When recomputation runs after ETD has passed, the computed status becomes `DEPARTED`. | No existing automated test identified for automatic departure (`06b_authoring_revised.md:946`). | Direct source + no automated test |
| FR-015 | Confirmed | Lazily load and render current status, delay-reason, and airline-distribution charts from aggregated flight data. | `02_discovery.md:36, 47, 120-122, 82` | `statistics.js:8-98`; `StatisticsController.java:22-25`; `StatisticsService.java:28-85` | Statistics tab requests one aggregated payload and renders three current-state charts. | `UC-007` Review Operational Statistics | `BO-005` Provide supervisory KPI/chart review | Self | Opening Statistics triggers lazy load from `/api/statistics` and renders the three declared visualizations. | No existing automated test identified for statistics aggregation/UI (`06b_authoring_revised.md:946-947`). | Direct source + no automated test |
| BR-008 | Confirmed | Seed sample flights and simulate live operational changes on every startup in the current implementation. | `02_discovery.md:14, 124-127` | `README.md:49-55`; `FlightSimulationService.java:35-129` | Startup creates demo flights, preset delays, and ongoing simulated updates. | `UC-008` Simulate Live Operational Activity | `BO-006` Support demo/training/reference evaluation | Current-state business baseline | Application startup produces the seeded reference dataset and activates scheduled simulation behavior. | No existing automated test identified for simulation/seeding (`06b_authoring_revised.md:946`). | Direct source + no automated test |
| NFR-003 | Confirmed | Recalculate statuses every 30 seconds and allow random delay changes between UI refresh cycles in the current implementation. | `02_discovery.md:39, 114, 127` | `FlightSimulationService.java:96-128` | Scheduler recomputes every 30 seconds and may inject random delays into eligible flights. | `UC-008` Simulate Live Operational Activity | `BO-006` Support demo/training/reference evaluation | Cross-cutting NFR / simulation baseline | Background simulation runs on the 30-second cadence and can alter board state between 15-second UI polls. | No existing automated test identified for scheduler/random delay behavior (`06b_authoring_revised.md:946`). | Direct source + no automated test |
| FR-016 | Confirmed | Allow a user to delete a flight permanently after a confirmation prompt in the current implementation. | `02_discovery.md:35, 49, 116-118, 81` | `dashboard.js:153-157`; `FlightController.java:66-69`; `FlightService.java:102-107` | Delete is a hard-remove flow with browser confirmation and HTTP 204 response. | `UC-009` Delete a Flight Record | `BO-003` Maintain flight records (current implementation) / governed by `BO-007` for future scope | Self | A confirmed delete removes the record from the repository and subsequent board/statistics retrievals. | No existing automated test identified for delete (`06b_authoring_revised.md:946`). | Direct source + no automated test |
| BR-009 | Assumed / Unresolved | Retain delete only as supervisory demo-data cleanup until audit, recovery, and retention controls are defined. | `02_discovery.md:116-118, 166-173` | no direct source — assumed; governance need inferred from current hard delete plus missing audit/persistence controls | The code performs hard delete today, but discovery surfaced no governed business process legitimizing it for authoritative records. | `UC-009` Delete a Flight Record | `BO-007` Define governance before any production rollout | Related current-state flow: `FR-016` | Stakeholders approve a supervisory-only demo cleanup rule or replace it with governed delete/retention/recovery controls (`SC-004`, `SC-005`). | No existing automated test; future approval/policy tests required. | Governance/gap trace only (no direct source — assumed) |
| RPT-001 | Confirmed | Report KPI values for total flights, on-time flights, delayed flights, cancelled flights, on-time percentage, and average delay. | `02_discovery.md:32, 67-70, 121-122` | `StatisticsDTO.java:11-49`; `StatisticsService.java:28-55`; `dashboard.js:37-48` | Aggregated KPI data is calculated server-side and displayed on the board. | `UC-007` Review Operational Statistics | `BO-005` Provide supervisory KPI/chart review | Related feature support: `FR-002`, `FR-015` | Current-state reporting shows the six KPI outputs and derives rates from the live in-memory dataset (`SC-002`). | No existing automated test identified for KPI aggregation/display (`06b_authoring_revised.md:946-947`). | Direct source + no automated test |
| RPT-002 | Confirmed | Provide visual reports showing flight distribution by status, delay reason, and airline. | `02_discovery.md:36, 67-70, 120-122` | `StatisticsService.java:56-85`; `statistics.js:25-98`; `index.html:163-190` | Statistics view renders status, delay-reason, and airline charts from one aggregated response. | `UC-007` Review Operational Statistics | `BO-005` Provide supervisory KPI/chart review | Related feature support: `FR-015` | All three chart groupings are available from the current operational dataset (`SC-002`). | No existing automated test identified for chart/report behavior (`06b_authoring_revised.md:946-947`). | Direct source + no automated test |
| RPT-003 | Confirmed | No historical exports, scheduled reports, downloadable files, or print/report-generation artifacts were found in the current reporting surface. | `02_discovery.md:18, 36, 67-70, 120-122` | Inspected reporting surface: `README.md`; `index.html:163-190`; `statistics.js:25-98`; `StatisticsController.java:22-25` | Reporting is limited to current on-screen KPI/chart output; no export or historical reporting path was surfaced. | `UC-007` Review Operational Statistics | `BO-005` Current scope is snapshot reporting only | Related feature support: `FR-015` | Stakeholders either accept snapshot-only reporting or add separate export/historical reporting requirements (`SC-002`). | No existing automated test; this is a scope/coverage observation rather than a tested behavior. | Direct inspected-surface trace + no automated test |
| NFR-001 | Confirmed | Auto-refresh the active dashboard or statistics view every 15 seconds. | `02_discovery.md:39, 87-89` | `app.js:6-31` | The active tab polls every 15 seconds to keep the live view current. | `UC-001` Monitor Current Departures; `UC-007` Review Operational Statistics | `BO-001` live monitoring; `BO-005` current statistics visibility | Cross-cutting NFR | While dashboard/statistics is active, the refresh loop runs on the 15-second cadence. | No existing automated test identified for polling cadence (`06b_authoring_revised.md:946-947`). | Direct source + no automated test |
| NFR-002 | Confirmed | Use a 300 ms debounce before issuing a new dashboard search request. | `02_discovery.md:45-46, 92` | `dashboard.js:230-236` | Search input waits 300 ms before calling the backend. | `UC-002` Search, Filter, and Prioritize Flights | `BO-002` Enable quick flight prioritization | Cross-cutting NFR for `FR-005` | Rapid typing does not issue a new search until the debounce interval elapses. | No existing automated test identified for debounce behavior (`06b_authoring_revised.md:946`). | Direct source + no automated test |
| NFR-011 | Unresolved | Define explicit throughput, latency, or availability SLAs beyond the implemented timer behaviors. | `02_discovery.md:149-154, 165-174` | no direct source — unresolved; discovery found runtime behaviors but no SLA artifact | Polling and simulation cadences are implemented, but no business-approved performance target was surfaced. | Cross-cutting operational readiness | `BO-007` Define governance before production rollout | Cross-cutting NFR gap | Stakeholders define the SLA/freshness target and UAT verifies the deployed solution meets it (`SC-001`). | No existing automated test; depends on future SLA definition and performance verification. | Governance/gap trace only (no direct source — unresolved) |
| SEC-001 | Assumed | Implement an approved role/permission matrix so only authorized identities can view and perform create/update/cancel/depart/delete actions. | `02_discovery.md:16, 23-27, 172` | no direct source — assumed; current code shows unrestricted access and no auth layer | Discovery confirmed every browser user can access read/write functions today, creating the need for future access control. | Cross-cutting across `UC-001` through `UC-009` | `BO-007` Define governance before production rollout | Security control governing `FR-008` through `FR-016` and statistics visibility | An approved role matrix is implemented and role-based tests prove unauthorized users cannot perform protected actions (`SC-003`). | No existing automated security/authorization tests. | Governance/gap trace only (no direct source — assumed) |
| SEC-002 | Assumed / Unresolved | Segregate duties so viewers are read-only, operators perform write actions, and delete is restricted to supervisory cleanup unless a stronger workflow is approved. | `02_discovery.md:24-27, 169, 172` | no direct source — assumed; discovery found implied roles but no enforced segregation model | Current UI/API exposure is uniform; role separation exists only as a BRD governance response to the gap. | Cross-cutting across `UC-001` through `UC-009` | `BO-007` Define governance before production rollout | Security/governance control related to `FR-012`, `FR-013`, `FR-016`, and `BR-009` | Approved segregation-of-duties policy is enforced and validated by role-based tests, especially around delete (`SC-003`, `SC-005`). | No existing automated security/segregation tests. | Governance/gap trace only (no direct source — assumed/unresolved) |
| SEC-003 | Unresolved | Define acceptable cross-origin browser access, TLS expectations, and network-boundary policy before production deployment. | `02_discovery.md:153-154, 172, 174` | no direct source — unresolved policy; current technical evidence shows open CORS in `WebConfig.java:17-23` | Discovery confirmed permissive CORS and no documented deployment security boundary. | Cross-cutting deployment/security readiness | `BO-007` Define governance before production rollout | Security control affecting all browser/API interactions | Stakeholders approve origin/TLS/network policy and deployment/security tests verify enforcement (`SC-003`, `SC-006`). | No existing automated network/security policy tests. | Governance/gap trace only (no direct source — unresolved) |
| NFR-004 | Confirmed | Provide structured user-visible error handling for validation failures, not-found conditions, and unexpected server errors. | `02_discovery.md:151, 157-162` | `GlobalExceptionHandler.java:20-45`; `api.js:12-23`; `dashboard.js:32-34` | Backend returns structured JSON error payloads and the frontend surfaces them through toasts/alerts. | Cross-cutting across load, create, update, cancel, depart, delete, and statistics flows | Cross-cutting support for `BO-001` through `BO-005` | Cross-cutting NFR; see also `OR-006` | Validation, not-found, and unexpected server failures remain visible to operators with usable message content. | No existing automated error-handling test identified (`06b_authoring_revised.md:945-947`). | Direct source + no automated test |
| DR-001 | Confirmed | Store flight number, airline, aircraft type, origin, destination, gate, scheduled departure, estimated departure, actual departure, status, delay reason, delay notes, and last updated timestamp on each flight record. | `02_discovery.md:55-61` | `Flight.java:12-25` | The core flight entity carries operational master data, timing data, state, notes, and update timestamp. | Supports `UC-003`, `UC-004`, `UC-005`, `UC-006`, `UC-007`, and `UC-009` | `BO-003`, `BO-004`, and `BO-005` | Foundational data requirement for `FR-008` through `FR-016` | The persisted flight object exposes the full operational field set required by the live board and statistics flows. | No existing automated field-coverage test identified. | Direct source + no automated test |
| DR-002 | Confirmed | Assign a UUID when a flight is saved without an existing identifier. | `02_discovery.md:55, 99-100` | `FlightRepository.java:20-26` | Repository save logic populates a UUID for newly created flights. | `UC-003` Register a New Flight for Monitoring | `BO-003` Add and maintain operational flight records | Supports `FR-008` and `FR-009` | Saving a new flight without an ID yields a generated UUID before the record is returned. | `TS-001` / `createFlightAssignsIdAndDefaults` (`FlightServiceTest.java:30-47`) directly covers ID assignment. | Direct source + automated test link |
| DR-003 | Confirmed / Unresolved | Treat `SCHEDULED`, `BOARDING`, `DEPARTED`, `DELAYED`, and `CANCELLED` as approved in-scope statuses while deferring `DIVERTED` until stakeholders define its workflow. | `02_discovery.md:40, 64, 141-144, 171` | `FlightStatus.java:6-23`; `dashboard.js:111-156`; `styles.css:112-117`; `statistics.js:25-30`; `FlightController.java:42-69` | The code supports six statuses, but discovery found no dedicated setter/process for `DIVERTED`. | `UC-001`, `UC-004`, `UC-005`, `UC-006`, `UC-007` | `BO-001` current board accuracy; governance under `BO-007` for diverted workflow approval | Supports `FR-003`, `FR-011`, `FR-012`, `FR-013`, `FR-014` | Operational workflows use the five approved statuses, and any future `DIVERTED` flow remains blocked until stakeholders define and verify it (`SC-005`). | No existing automated test identified for status-governance coverage. | Partial direct trace + unresolved governance gap |
| DR-004 | Confirmed | Support the delay reasons Weather, Air Traffic Control, Mechanical Issue, Crew Availability, Security, Late Arriving Aircraft, Fueling, Catering, Baggage Handling, and Other. | `02_discovery.md:65, 103-104` | `DelayReason.java:6-27`; `index.html:268-282` | The ETD workflow and domain model share one controlled list of delay reason values. | `UC-004` Update ETD and Record Delay Context | `BO-004` Capture disruption context | Supports `FR-010` and `FR-011` | Operators can select only the enumerated delay reasons and the saved flight retains the selected reason. | `TS-002` / `updateEtdMarksFlightAsDelayed` (`FlightServiceTest.java:49-69`) verifies storing `DelayReason.WEATHER`. | Direct source + automated test link |
| DR-005 | Confirmed | Provide statistics data fields for totalFlights, onTimeCount, delayedCount, cancelledCount, onTimePercentage, averageDelayMinutes, countByStatus, countByDelayReason, and countByAirline. | `02_discovery.md:67-70, 82` | `StatisticsDTO.java:11-49`; `StatisticsService.java:28-85` | Statistics endpoint returns both KPI values and chart groupings from the current dataset. | `UC-007` Review Operational Statistics | `BO-005` Provide supervisory KPI/chart review | Supports `FR-015`, `RPT-001`, and `RPT-002` | The statistics payload exposes the full KPI + distribution field set needed by the current UI. | No existing automated test identified for statistics DTO/service coverage. | Direct source + no automated test |
| NFR-006 | Unresolved | Define explicit business timezone handling for date-time entry, display, serialization, and validation. | `02_discovery.md:152, 168` | no direct source — unresolved; discovery found UTC server serialization and browser-local `datetime-local` inputs but no business timezone policy | Current create/update time entry is browser-local while API serialization is UTC, leaving business interpretation unspecified. | Cross-cutting across `UC-003`, `UC-004`, `UC-006`, and `UC-007` | `BO-007` Define governance before production rollout | Cross-cutting NFR gap affecting timing-related FRs | Stakeholders approve timezone rules for input/display/API validation and the solution is verified against that policy. | No existing automated timezone-policy tests. | Governance/gap trace only (no direct source — unresolved) |
| NFR-007 | Assumed / Unresolved | Define and approve persistence, restart-recovery, and record-durability requirements before any production rollout. | `02_discovery.md:15, 166, 173-174` | no direct source — assumed/unresolved; discovery found in-memory-only storage and demo/reference positioning | Current records disappear on restart, so production durability expectations remain undefined. | Cross-cutting production-readiness prerequisite | `BO-007` Define governance before production rollout | Cross-cutting NFR gap; relates to `AUD-002` and delete governance | Production scope is not approved until persistence/recovery expectations are documented and verified (`SC-004`, `SC-006`). | No existing automated persistence/recovery tests. | Governance/gap trace only (no direct source — assumed/unresolved) |
| AUD-001 | Assumed / Unresolved | Capture an audit trail for create, ETD change, cancellation, departure, and delete actions with actor, timestamp, prior state, new state, and supplied reason/notes if productionization is approved. | `02_discovery.md:167, 173` | no direct source — assumed/unresolved; discovery explicitly found no audit/history trail in current artifacts | The current solution mutates in-memory records without any historical change log. | Cross-cutting across `UC-003`, `UC-004`, `UC-005`, `UC-006`, and `UC-009` | `BO-007` Define governance before production rollout | Audit control for operational write flows | If production scope is approved, audit records capture who changed what, when, and why, and audit verification is part of operational testing (`SC-004`). | No existing automated audit-trail tests. | Governance/gap trace only (no direct source — assumed/unresolved) |
| AUD-002 | Assumed / Unresolved | Define retention, recovery, and historical-record expectations for deleted and restarted operational data if productionization is approved. | `02_discovery.md:166-167, 173` | no direct source — assumed/unresolved; discovery found no persistence, recovery, or retained history mechanism | Delete and restart currently remove operational history permanently. | Cross-cutting production-readiness prerequisite | `BO-007` Define governance before production rollout | Retention/recovery control tied to `FR-016`, `BR-009`, and `NFR-007` | Stakeholders approve retention/recovery/purge rules and production testing verifies those controls before go-live (`SC-004`). | No existing automated recovery/retention tests. | Governance/gap trace only (no direct source — assumed/unresolved) |
| OR-006 | Confirmed | Preserve clear operator-visible error messaging for validation and backend failures. | `02_discovery.md:151, 157-162` | `GlobalExceptionHandler.java:20-45`; `api.js:12-23`; `dashboard.js:32-34`; `statistics.js:20-22` | Frontend error handling composes backend JSON error payloads into visible toasts/alerts across user flows. | Cross-cutting across `UC-001` through `UC-007` and `UC-009` | Cross-cutting support for `BO-001` through `BO-005` | Cross-cutting requirement aligned with `NFR-004` | Operators continue to receive clear visible feedback for load failures, validation failures, not-found errors, and action failures. | No existing automated error-message preservation test identified. | Direct source + no automated test |

## Notes

- `TS-001` and `TS-002` are the only automated tests surfaced in the inspected test surface (`FlightServiceTest.java:17-70`).
- Rows marked `no direct source` are still traceable to discovery gap findings, but they depend on stakeholder governance decisions rather than implemented code paths.
- No requirement extracted from the revised BRD was completely untraceable; unresolved items were traceable as explicit discovery gaps.
