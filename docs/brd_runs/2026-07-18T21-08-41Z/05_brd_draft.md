# ETD Airlines — Departure Operations System
# Business Requirements Document (Reverse-Engineered)

---

## Front Matter

| Field | Value |
|---|---|
| **Document Title** | ETD Airlines Departure Operations System — Business Requirements Document |
| **Document Version** | 1.0 (Draft — Reverse-Engineered) |
| **Date** | 2026-07-18 |
| **Run ID** | `2026-07-18T21-08-41Z` |
| **Authors** | Generated by BRD reverse-engineering pipeline run `2026-07-18T21-08-41Z` (brd-authoring agent). No human author names assigned. |
| **Repository** | `PadmavathyBala/SeleniumPractise_OpenCartProject`, subdirectory `etd-airlines_1` |

### Document History

| Version | Date | Author | Change Summary |
|---|---|---|---|
| 1.0 | 2026-07-18 | Pipeline run `2026-07-18T21-08-41Z` | Initial reverse-engineered draft |

### Approvals

*This table is intentionally blank. Sign-off is a human action; no agent may
populate names or signatures here.*

| Stakeholder | Title | Signature | Date |
|---|---|---|---|
| | | | |
| | | | |

---

## 1. Introduction

### 1.1 Purpose of the Document

This document is a reverse-engineered Business Requirements Document (BRD) for the
**ETD Airlines Departure Operations System** (`etd-airlines_1`). It was produced
automatically by analysing the source code, configuration files, UI assets, and unit
tests of the application. Its purpose is to reconstruct what the system was built to
do so that stakeholders can validate, extend, and govern the solution.

This BRD does **not** represent requirements that were agreed upfront; it represents
requirements that are *implied by the existing implementation*. Stakeholders should
review it for accuracy and fill in gaps (especially items labelled **Assumed** or
**Unresolved**) before treating it as a governing document.

### 1.2 Document Conventions, Terms, and Definitions

#### 1.2.1 Confidence Labels

Every requirement, rule, and workflow description in this document carries one of the
following labels:

| Label | Meaning |
|---|---|
| **Confirmed** | Directly supported by code, UI, configuration, tests, or documentation. A specific file/class/method citation is provided. |
| **Inferred** | Derived from multiple technical observations that together imply a behaviour, but no single source states it directly. The inference chain is briefly explained. |
| **Assumed** | A reasonable guess where evidence is thin. Requires stakeholder confirmation before being treated as final. |
| **Unresolved** | Insufficient evidence exists to make a reliable statement. The gap is stated plainly. |

No requirement is stated without one of these labels. No Assumed or Unresolved item
has been upgraded to Confirmed without supporting evidence.

#### 1.2.2 Domain Terms

| Term | Definition |
|---|---|
| ETD | Estimated Time of Departure — the current best estimate of when a flight will depart. |
| STD | Scheduled Time of Departure — the originally planned departure time. |
| ATD | Actual Time of Departure — the time the aircraft actually left the gate. |
| IATA code | Three-letter International Air Transport Association airport identifier (e.g., DFW, LAX). |
| Flight record | A single entity representing one flight's operational data. |
| Delay threshold | 15 minutes — minimum ETD slippage for a flight to be classified as "delayed". |
| In-memory store | Data held in JVM RAM, not persisted to a database. Lost on restart. |

### 1.3 Intended Audience and Reading Suggestions

| Audience | Recommended Sections |
|---|---|
| Business stakeholders / product owners | §2 (Overview), §7 (Success Criteria), Appendix C (Issues) |
| Operations staff (gate agents, supervisors) | §2.5 (Stakeholders), §3 (System Features) |
| Software architects / developers | §3 (Features), §4 (Interfaces), §5 (NFRs), Appendix F (Traceability) |
| QA engineers | §3 (use cases), Appendix E (Test Scenarios) |

### 1.4 Scope

#### 1.4.1 Project Scope

**In Scope**
- Web-based single-page application for managing departure-board data for a single
  airport operations scenario.
- REST API exposing flight CRUD, ETD update, and statistics operations.
- Browser-based dashboard with real-time-polling refresh (15 s), ETD update modal,
  and visual statistics charts.
- Automated status computation based on current time vs ETD.
- Demo simulation that seeds and periodically modifies flight data.

**Out of Scope** (current version — **Confirmed** absent from codebase)
- User authentication and authorisation.
- Persistent database storage.
- Real-time push notifications (WebSocket / Server-Sent Events).
- Integration with external ATC, weather, or aircraft-tracking systems.
- Multi-airport or multi-tenant operations.
- Mobile native application.
- Audit log of ETD changes.
- Reporting exports (CSV, PDF).

#### 1.4.2 BRD Scope

This document covers the functional and non-functional requirements of the
`etd-airlines_1` subdirectory only. It does not cover the `OpenCartV001` Selenium
test project or the `etd-airlines` directory (structurally similar predecessor
version).

### 1.5 References

| Ref | Description |
|---|---|
| [R1] | Repository: `PadmavathyBala/SeleniumPractise_OpenCartProject`, subdirectory `etd-airlines_1` |
| [R2] | Run ID: `2026-07-18T21-08-41Z` |
| [R3] | `etd-airlines_1/README.md` — project documentation |
| [R4] | `etd-airlines_1/agents/Grounding Documents/Business Requirements Document Template.doc` — structural template |
| [R5] | `etd-airlines_1/agents/Grounding Documents/Sample BRD_template.pdf` — structural template |

---

## 2. Overall Description

### 2.1 Executive Summary

The ETD Airlines Departure Operations System is a lightweight, browser-based web
application designed to help airport operations staff track and manage estimated
departure times across a set of flights on a departure board. Operators can view
all current flights, update ETDs when delays occur, mark flights as departed or
cancelled, add new flights, and view aggregated statistics including on-time
performance and delay-reason breakdowns. The system is built as a Spring Boot REST
API with a single-page HTML/JavaScript front-end.  
**Confirmed** — derived from `README.md`, controllers, and UI assets.

### 2.2 Business Background / Current-State Overview

Airport departure operations require real-time visibility into flight status and ETD
changes. Operations staff need to track delays across multiple airlines and flights,
communicate changes to other departments, and record actual departure times. Without
a centralised system, operations staff would rely on manual tracking or external
FIDS (Flight Information Display Systems) that cannot be updated by local staff.

This application provides a self-contained, locally hosted departure management tool
that can be run on any machine with Java 17 and a browser.  
**Inferred** — from README deployment instructions and the absence of any external
data integration.

### 2.3 Business Objectives

| # | Objective | Confidence |
|---|---|---|
| BO-1 | Provide operations staff with a real-time view of all departure flights and their current ETDs. | Confirmed — Dashboard auto-refreshes every 15 s |
| BO-2 | Enable gate agents to update ETDs and record delay reasons without leaving the tool. | Confirmed — ETD update modal in dashboard.js |
| BO-3 | Enable accurate recording of actual departure times. | Confirmed — `markDeparted()` records `actualDeparture` |
| BO-4 | Provide supervisors with aggregated on-time performance and delay-reason analytics. | Confirmed — StatisticsService, Statistics tab |
| BO-5 | Reduce manual data entry errors through input validation. | Confirmed — Bean validation on CreateFlightRequest, UpdateEtdRequest |
| BO-6 | Support easy deployment in an Eclipse-based Java development environment for demonstration purposes. | Confirmed — README import/run instructions, Eclipse metadata |

### 2.4 Product Features (High-Level Summary)

| Feature | Summary |
|---|---|
| F1 — Flight Board Dashboard | Tabular view of all flights with status badges, delay indicators, KPI counters, and per-row actions. |
| F2 — ETD / Delay Management | Modal to update ETD, select delay reason, and enter notes; auto-recomputes status. |
| F3 — Departure Recording | One-click "Mark Departed" action capturing actual departure time. |
| F4 — Flight Cancellation | One-click cancel with optional notes. |
| F5 — New Flight Entry | Form-based creation of flight records with validation. |
| F6 — Flight Deletion | Permanent removal of a flight record. |
| F7 — Free-Text Search | Search flights across number, route, and airline name. |
| F8 — Statistics Dashboard | Visual charts for on-time performance, delay reasons, and airline breakdown. |
| F9 — Live Simulation | Background job that periodically applies random delays for demo purposes. |

### 2.5 Stakeholders and User Classes

| Role | Department (Assumed) | Responsibilities | Capabilities in System |
|---|---|---|---|
| Gate Agent | Airport Operations | Manage individual flight departures; update ETDs; record departures. | Update ETD, Mark Departed, Cancel Flight |
| Operations Supervisor | Airport Operations Management | Oversee all departures; add/remove flights from board. | All Gate Agent + Add Flight, Delete Flight |
| Manager / Viewer | Airport Management | Monitor on-time performance and delay patterns. | View Dashboard and Statistics (read-only — Assumed, no RBAC enforced) |
| System Administrator | IT / DevOps | Deploy and maintain the application. | Not represented in UI |

**Note:** Role separation is **Assumed**. The current codebase has no authentication
or access control. All users share the same unrestricted interface.

### 2.6 Operating Environment

**Confirmed** — all items cited to `pom.xml` and `application.properties`.

| Component | Specification |
|---|---|
| Server runtime | Java 17+ |
| Application framework | Spring Boot 3.2.5 |
| Build tool | Apache Maven 3.6+ |
| Default HTTP port | 8080 |
| Client runtime | Modern web browser (Chrome, Firefox, Edge) |
| Client dependencies | Bootstrap 5 (CDN), Bootstrap Icons (CDN), Chart.js (CDN) |
| Data store | In-memory (`ConcurrentHashMap`) — non-persistent |

### 2.7 Design and Implementation Constraints

| Constraint | Confidence |
|---|---|
| No persistent storage — all data lives in JVM memory and is lost on restart. | Confirmed — `FlightRepository.java` |
| No user authentication or session management. | Confirmed — no Spring Security in `pom.xml` |
| Single-node deployment only (no clustering support for in-memory store). | Inferred — `ConcurrentHashMap` is local-JVM; no distributed cache |
| Browser clients poll the server every 15 s (no server push). | Confirmed — `app.js` `REFRESH_INTERVAL_MS` |
| Date serialization is UTC ISO-8601; local timezone display depends on browser locale. | Confirmed — `application.properties`; `dashboard.js:formatTime()` uses `toLocaleString` |

### 2.8 Assumptions, Dependencies, and Risks

| # | Type | Statement | Confidence |
|---|---|---|---|
| A1 | Assumption | The system is intended for a single airport's departure operations. | Inferred — no multi-airport data model |
| A2 | Assumption | The simulation job (`FlightSimulationService`) is a demo artefact and would be replaced in production. | Inferred — code comment in `EtdCalculationService` |
| A3 | Assumption | External network access for CDN assets (Bootstrap, Chart.js) is available in the deployment environment. | Inferred — CDN links in `index.html`; no bundling |
| A4 | Dependency | Java 17 JDK must be installed on the server host. | Confirmed — `pom.xml` |
| A5 | Risk | Data loss on application restart due to in-memory store. | Confirmed — `FlightRepository` |
| A6 | Risk | No access control means any network-accessible user can modify flight data. | Confirmed — no authentication |
| A7 | Risk | Single-node deployment creates a single point of failure. | Inferred — no HA architecture present |

---

## 3. System Features

### 3.1 Feature: Flight Board Dashboard

#### 3.1.1 Description

**Confirmed** — `static/js/dashboard.js`, `static/index.html`

A tabular view of all flights currently in the system, updated automatically every
15 seconds. Each row shows flight number, airline, origin→destination route, gate,
scheduled departure, estimated departure, delay indicator, status badge, and delay
reason. A set of KPI counters above the table summarises totals.

#### 3.1.2 Business Rules

| BR-ID | Rule | Confidence |
|---|---|---|
| BR-01 | The dashboard shall auto-refresh every 15 seconds while the Dashboard tab is active. | Confirmed — `app.js:REFRESH_INTERVAL_MS=15000` |
| BR-02 | A flight is displayed with orange/red visual styling if it is delayed (≥15 min). | Confirmed — `dashboard.js:renderRow()` `row-delayed` class |
| BR-03 | A departed flight row shall be visually muted. | Confirmed — `dashboard.js:renderRow()` `row-departed` class |
| BR-04 | A cancelled flight row shall be displayed with strikethrough styling. | Confirmed — `dashboard.js:renderRow()` `row-cancelled` class |
| BR-05 | The delay badge shall show "+Nm" for delayed flights and "on time" otherwise. | Confirmed — `dashboard.js:renderRow()` delay badge logic |

#### 3.1.3 Functional Requirements

FR-007, FR-025, FR-026, FR-027 (see §4 requirements table in `04_requirements_extraction.md`)

#### 3.1.4 Use Cases

---

**Use Case ID:** UC-001  
**Use Case Name:** View and Filter Flight Board  
**Actors:** Gate Agent, Operations Supervisor, Manager  
**Description:** A user views the live departure board, optionally filtering by status or sorting by a chosen field. **Confirmed**  
**Trigger:** User navigates to the Dashboard tab or the 15-second auto-refresh fires.  
**Preconditions:** The application is running; at least one flight exists. **Confirmed**  
**Postconditions:** The flight table is populated with current data. **Confirmed**  
**Normal Flow:**
1. Browser sends `GET /api/flights` (with optional `?search=` parameter).
2. Server returns sorted list of `FlightDTO` objects.
3. Dashboard calls `GET /api/statistics` for KPI panel.
4. UI renders table rows and KPI counters.
5. User optionally selects a status filter or sort order; table re-renders client-side.
**Alternative Flow:** User types in the search box; a debounced (300 ms) API call is sent with the search term.  
**Exceptions:** If the API call fails, a "danger" toast notification is shown. **Confirmed** — `dashboard.js:refresh()` catch block.  
**Includes:** UC-002 (update ETD), UC-003 (mark departed), UC-004 (cancel), UC-005 (delete)  
**Priority:** High  
**Frequency of Use:** Continuous (every 15 seconds, plus manual interactions)  
**Business Rules:** BR-01 through BR-05  
**Special Requirements:** Client-side only filtering/sorting after initial data load — no additional API calls for sort/filter changes.  
**Assumptions:** User has a browser with JavaScript enabled. **Assumed**  
**Notes and Issues:** No pagination — all flights loaded at once. Could be a performance issue with large flight counts. **Inferred**

---

### 3.2 Feature: ETD / Delay Management

#### 3.2.1 Description

**Confirmed** — `service/FlightService.java:updateEtd()`, `static/js/dashboard.js:openEtdModal()`

Operators can update a flight's estimated departure time through a modal dialog,
optionally categorising the delay reason and adding free-text notes.

#### 3.2.2 Business Rules

| BR-ID | Rule | Confidence |
|---|---|---|
| BR-06 | A new ETD cannot be set to a time more than 5 minutes in the past. | Confirmed — `FlightService.updateEtd()` |
| BR-07 | After ETD update, flight status is recomputed automatically (SCHEDULED/BOARDING/DELAYED). | Confirmed — `FlightService.updateEtd()` → `EtdCalculationService.computeStatus()` |
| BR-08 | ETD ≥ 15 min past STD marks the flight as delayed. | Confirmed — `Flight.isDelayed()` |
| BR-09 | Within 30 minutes of ETD, a non-delayed flight transitions to BOARDING. | Confirmed — `EtdCalculationService.computeStatus()` |

#### 3.2.3 Functional Requirements

FR-010, FR-011, FR-012, FR-013, FR-014, FR-015, FR-016

#### 3.2.4 Use Cases

---

**Use Case ID:** UC-002  
**Use Case Name:** Update Flight ETD  
**Actors:** Gate Agent, Operations Supervisor  
**Description:** Operator updates the estimated departure time and records the reason for delay. **Confirmed**  
**Trigger:** Operator receives information about a departure change and clicks the clock-icon button on a flight row.  
**Preconditions:** Flight exists in SCHEDULED, BOARDING, or DELAYED status. **Inferred** — no guard in code but terminal states are skipped.  
**Postconditions:** `estimatedDeparture`, `delayReason`, `delayNotes`, `status`, and `lastUpdated` are updated. **Confirmed**  
**Normal Flow:**
1. Operator clicks clock button on a flight row.
2. Modal opens pre-populated with current ETD, reason, and notes.
3. Operator adjusts datetime, selects a delay reason, and enters notes.
4. Operator clicks Save.
5. Browser sends `PUT /api/flights/{id}/etd` with `UpdateEtdRequest` payload.
6. Server validates new ETD (not >5 min in past).
7. Server updates ETD fields and recomputes status.
8. Server returns updated `FlightDTO`; modal closes; dashboard refreshes.
**Alternative Flow:** Operator cancels the modal — no API call made.  
**Exceptions:** New ETD is more than 5 minutes in the past → 400 Bad Request → "danger" toast displayed. **Confirmed**  
**Includes:** —  
**Priority:** High  
**Frequency of Use:** Multiple times per hour during active operations  
**Business Rules:** BR-06, BR-07, BR-08, BR-09  
**Special Requirements:** Delay reason is selected from a fixed enumeration (DelayReason enum). **Confirmed**  
**Assumptions:** The modal's datetime input uses the user's local browser timezone for display, but the server stores in UTC. **Inferred**  
**Notes and Issues:** OQ-007 — no guard prevents updating a DEPARTED or CANCELLED flight via direct API call.

---

### 3.3 Feature: Departure Recording

#### 3.3.1 Description

**Confirmed** — `service/FlightService.java:markDeparted()`, `static/js/dashboard.js:attachRowHandlers()`

Operators mark a flight as physically departed, capturing the actual departure time.

#### 3.3.2 Business Rules

| BR-ID | Rule | Confidence |
|---|---|---|
| BR-10 | Marking a flight departed sets status = DEPARTED and records actualDeparture = server time. | Confirmed — `FlightService.markDeparted()` |
| BR-11 | The UI requires a confirmation before sending the depart request. | Confirmed — `dashboard.js` `confirm()` call |

#### 3.3.3 Functional Requirements

FR-020

#### 3.3.4 Use Cases

---

**Use Case ID:** UC-003  
**Use Case Name:** Mark Flight Departed  
**Actors:** Gate Agent  
**Description:** Gate agent confirms a flight has physically departed the gate. **Confirmed**  
**Trigger:** Aircraft pushes back from gate.  
**Preconditions:** Flight exists and is not already in a terminal state. **Inferred**  
**Postconditions:** `status = DEPARTED`, `actualDeparture = now()`. **Confirmed**  
**Normal Flow:**
1. Operator clicks aeroplane-icon button on flight row.
2. Browser shows confirmation dialog.
3. Operator confirms.
4. Browser sends `POST /api/flights/{id}/depart`.
5. Server records departure and returns updated DTO.
6. Row styling changes to departed (muted). Dashboard refreshes.
**Alternative Flow:** Operator cancels confirmation — no action taken.  
**Exceptions:** Flight not found → 404. **Confirmed**  
**Includes:** —  
**Priority:** High  
**Frequency of Use:** Once per flight  
**Business Rules:** BR-10, BR-11  
**Special Requirements:** —  
**Assumptions:** —  
**Notes and Issues:** No guard against departing an already-cancelled flight (OQ-007).

---

### 3.4 Feature: Flight Cancellation

#### 3.4.1 Description

**Confirmed** — `service/FlightService.java:cancelFlight()`, `static/js/dashboard.js`

Operators cancel a flight and optionally record a cancellation note.

#### 3.4.2 Business Rules

| BR-ID | Rule | Confidence |
|---|---|---|
| BR-12 | Cancelling a flight sets status = CANCELLED. | Confirmed — `FlightService.cancelFlight()` |
| BR-13 | An optional free-text cancellation note is stored in `delayNotes`. | Confirmed — `FlightService.cancelFlight()` |
| BR-14 | The UI prompts for an optional note before sending the cancel request. | Confirmed — `dashboard.js` `prompt()` call |

#### 3.4.3 Functional Requirements

FR-021

#### 3.4.4 Use Cases

---

**Use Case ID:** UC-004  
**Use Case Name:** Cancel Flight  
**Actors:** Gate Agent, Operations Supervisor  
**Description:** Operator cancels a flight with an optional note. **Confirmed**  
**Trigger:** Operational decision to cancel a flight.  
**Preconditions:** Flight exists in the system. **Confirmed**  
**Postconditions:** `status = CANCELLED`; optional note stored. **Confirmed**  
**Normal Flow:**
1. Operator clicks X-octagon button on flight row.
2. Browser prompts for cancellation note.
3. Browser sends `POST /api/flights/{id}/cancel` with `{"notes":"..."}`.
4. Server sets status; returns updated DTO. Dashboard refreshes.
**Alternative Flow:** User dismisses prompt (`null`) — no action taken. **Confirmed** — `if (notes === null) return;`  
**Exceptions:** Flight not found → 404.  
**Includes:** —  
**Priority:** High  
**Frequency of Use:** Occasional  
**Business Rules:** BR-12, BR-13, BR-14  
**Special Requirements:** —  
**Assumptions:** —  
**Notes and Issues:** Empty string note is a valid input (passes the `!== null` guard). **Confirmed**

---

### 3.5 Feature: New Flight Entry

#### 3.5.1 Description

**Confirmed** — `controller/FlightController.java:create()`, `dto/CreateFlightRequest.java`, `static/js/addFlight.js`

Operators add a new flight to the departure board via a validated form.

#### 3.5.2 Business Rules

| BR-ID | Rule | Confidence |
|---|---|---|
| BR-15 | Flight number must match `^[A-Z]{2,3}\d{1,4}$`. | Confirmed — `CreateFlightRequest` `@Pattern` |
| BR-16 | Origin and destination must be exactly 3 uppercase letters. | Confirmed — `CreateFlightRequest` `@Pattern` |
| BR-17 | Flight number + same scheduled time = duplicate → rejected. | Confirmed — `FlightService.createFlight()` |
| BR-18 | On creation, ETD = STD and status = SCHEDULED. | Confirmed — `FlightService.createFlight()` |
| BR-19 | The default scheduled time in the UI is 1 hour from now. | Confirmed — `addFlight.js:init()` |

#### 3.5.3 Functional Requirements

FR-001, FR-002, FR-003, FR-004, FR-005, FR-006

#### 3.5.4 Use Cases

---

**Use Case ID:** UC-005  
**Use Case Name:** Add New Flight  
**Actors:** Operations Supervisor  
**Description:** Supervisor adds a new flight record to the departure board. **Confirmed**  
**Trigger:** A new flight needs to be tracked (charter, late addition, etc.)  
**Preconditions:** User is on the "Add Flight" tab.  
**Postconditions:** New flight visible on Dashboard with status SCHEDULED and ETD = STD. **Confirmed**  
**Normal Flow:**
1. Operator fills in Flight Number, Airline, Origin, Destination, optional Gate and Aircraft Type, Scheduled Departure.
2. Operator clicks "Add Flight".
3. Browser sends `POST /api/flights`.
4. Server validates and creates flight; returns `201 Created` with FlightDTO.
5. Success alert shown; form reset; Dashboard refreshes.
**Alternative Flow:** Operator clicks Reset — form cleared.  
**Exceptions:** Validation failure → 400 with field errors displayed in alert. Duplicate flight → 400.  
**Includes:** —  
**Priority:** Medium  
**Frequency of Use:** Occasional  
**Business Rules:** BR-15 through BR-19  
**Special Requirements:** Form submits values as JSON, not form-encoded. **Confirmed** — `addFlight.js:submit()` |  
**Assumptions:** —  
**Notes and Issues:** Front-end converts flight number and IATA codes to uppercase before submit. **Confirmed** — `.toUpperCase()` calls in `addFlight.js`

---

### 3.6 Feature: Statistics Dashboard

#### 3.6.1 Description

**Confirmed** — `service/StatisticsService.java`, `static/js/statistics.js`

A visual analytics tab showing aggregated departure performance metrics.

#### 3.6.2 Business Rules

| BR-ID | Rule | Confidence |
|---|---|---|
| BR-20 | On-time percentage = (onTimeCount / totalFlights) × 100, rounded to 1 decimal. | Confirmed — `StatisticsService.buildStatistics()` |
| BR-21 | Average delay is computed only over flights where `isDelayed() == true`. | Confirmed — `StatisticsService.buildStatistics()` stream filter |
| BR-22 | An "on-time" flight is one that is not delayed and not cancelled. | Confirmed — `StatisticsService.buildStatistics()` |

#### 3.6.3 Functional Requirements

FR-022, FR-023, FR-024

#### 3.6.4 Use Cases

---

**Use Case ID:** UC-006  
**Use Case Name:** View Operational Statistics  
**Actors:** Manager, Operations Supervisor  
**Description:** User views aggregated charts of on-time performance, delay reasons, and airline breakdown. **Confirmed**  
**Trigger:** User navigates to the Statistics tab.  
**Preconditions:** Application is running; flights are present. **Confirmed**  
**Postconditions:** Three charts rendered from live statistics data.  
**Normal Flow:**
1. User clicks Statistics tab.
2. `shown.bs.tab` event fires; `Statistics.refresh()` called.
3. Browser fetches `GET /api/statistics`.
4. Server aggregates all flights and returns `StatisticsDTO`.
5. Three Chart.js charts rendered: status doughnut, delay reason bar, airline horizontal bar.
**Alternative Flow:** —  
**Exceptions:** API failure → "danger" toast.  
**Includes:** —  
**Priority:** Medium  
**Frequency of Use:** Several times per shift  
**Business Rules:** BR-20, BR-21, BR-22  
**Special Requirements:** Charts require Chart.js CDN to be accessible. **Inferred**  
**Assumptions:** Charts are re-created (destroyed and rebuilt) on each refresh. **Confirmed** — `statistics.js` `chart.destroy()` calls  
**Notes and Issues:** No export or print functionality. **Confirmed** absent.

---

## 4. External Interface Requirements

### 4.1 User Interface

**Confirmed** — `static/index.html`, `static/js/*.js`, `static/css/styles.css`

- Single-page application (SPA) with Bootstrap 5 tab navigation.
- Three tabs: Dashboard, Statistics, Add Flight.
- Toast notification system for success/failure feedback.
- Modal dialog for ETD updates.
- Responsive layout (Bootstrap grid).
- Flight status visually encoded using color-coded badge classes:
  `status-SCHEDULED`, `status-BOARDING`, `status-DEPARTED`, `status-DELAYED`,
  `status-CANCELLED`, `status-DIVERTED`.

### 4.2 Hardware Interfaces

**Unresolved** — no hardware-specific interfaces identified. The application runs as
a standard HTTP server on any machine with Java 17.

### 4.3 Software Interfaces (APIs and Integrations)

| Interface | Direction | Protocol | Notes |
|---|---|---|---|
| REST API (internal) | Browser → Server | HTTP/JSON | All endpoints documented in §2.4 / Discovery §4 |
| Bootstrap 5 | Client-side | CDN (HTTPS) | UI framework | **Confirmed** |
| Bootstrap Icons | Client-side | CDN (HTTPS) | Icon set | **Confirmed** |
| Chart.js | Client-side | CDN (HTTPS) | Chart rendering | **Confirmed** |
| Spring Actuator | External monitoring | HTTP/JSON | `/actuator/health`, `/actuator/info`, `/actuator/metrics` | **Confirmed** |
| ATC / Weather / Aircraft tracking | Inbound | Not implemented | Placeholder — see `EtdCalculationService` comment | **Unresolved** |

### 4.4 Communications Interfaces

- Protocol: HTTP (no TLS configured in default setup). **Confirmed** — `application.properties` has no SSL settings.
- Data format: JSON for API; UTF-8 HTML for UI. **Confirmed**

### 4.5 Reporting Requirements

No automated reports, scheduled exports, or email notifications are implemented.
**Confirmed** — absent from codebase.

---

## 5. Other Nonfunctional Requirements

### 5.1 Performance

| NFR-ID | Requirement | Confidence |
|---|---|---|
| NFR-009 | The dashboard shall auto-refresh at a 15-second interval. | Confirmed — `app.js` |
| NFR-010 | Background simulation updates run every 30 seconds. | Confirmed — `@Scheduled(fixedRate=30_000)` |
| NFR-011 | No explicit throughput or latency SLAs are defined in the codebase. | Unresolved |

### 5.2 Security

| NFR-ID | Requirement | Confidence |
|---|---|---|
| NFR-012 | The current version has no authentication, authorisation, or session management. | Confirmed — no Spring Security dependency |
| NFR-013 | No HTTPS / TLS is configured by default. | Confirmed — no SSL settings in `application.properties` |
| NFR-014 | No CSRF protection is configured. | Confirmed — not present in codebase |
| NFR-015 | HTML output is escaped client-side for display fields. | Confirmed — `dashboard.js:escapeHtml()` function |

### 5.3 Data Requirements

| NFR-ID | Requirement | Confidence |
|---|---|---|
| NFR-016 | Data is entirely in-memory. All flight records are lost on application restart. | Confirmed — `FlightRepository` uses `ConcurrentHashMap` |
| NFR-017 | The repository supports concurrent read/write access via `ConcurrentHashMap`. | Confirmed — `FlightRepository.java` |
| NFR-018 | Flight IDs are UUID v4 strings generated at first save. | Confirmed — `FlightRepository.save()` |

### 5.4 Software Quality Attributes

| Attribute | Statement | Confidence |
|---|---|---|
| Maintainability | Layered architecture (Controller / Service / Repository / Model / DTO) separates concerns clearly. | Confirmed — package structure |
| Extensibility | README explicitly lists JPA migration, WebSocket, Spring Security, and weather feed as near-term extensions. | Confirmed — README "Extending the Application" section |
| Testability | Unit tests for `FlightService` demonstrate plain-Java testability without Spring context. | Confirmed — `FlightServiceTest.java` |
| Observability | Spring Actuator endpoints exposed for health and metrics. | Confirmed — `application.properties` |

---

## 6. Other Requirements

- Eclipse IDE import instructions provided in README for developer onboarding.
  **Confirmed** — `README.md`.
- Maven wrapper not included; requires system Maven 3.6+. **Confirmed** — README, no `mvnw` file.
- Troubleshooting notes for port conflicts and Maven proxy configuration provided.
  **Confirmed** — README "Troubleshooting" section.

---

## 7. Success Criteria

*A reverse-engineered BRD cannot derive real quantitative targets from code alone.
All items below are **Assumed** unless the codebase contains an explicit SLA or
target (noted as Confirmed).*

| SC-ID | Criterion | Confidence |
|---|---|---|
| SC-01 | The departure board accurately reflects the latest ETDs and statuses within 15 seconds of any change. | Inferred — 15 s polling interval; assumes network latency is negligible |
| SC-02 | ETD updates are persisted (in-memory) and reflected on all connected browsers within the next poll cycle. | Confirmed — in-memory store is shared across requests |
| SC-03 | Validation prevents malformed flight numbers and IATA codes from entering the system. | Confirmed — Bean validation on `CreateFlightRequest` |
| SC-04 | The on-time percentage and average delay shown in the Statistics tab accurately reflect current in-memory data. | Confirmed — `StatisticsService` computes directly from repository |
| SC-05 | A 50% reduction in manual data entry errors (compared to baseline). | Unresolved — no baseline data available from code |
| SC-06 | System uptime of 99.9% during operational hours. | Unresolved — no SLA defined in codebase |

---

## 8. Approval and Sign-off

*This table is intentionally blank. Sign-off is a human action.*

| Stakeholder | Title | Signature | Date |
|---|---|---|---|
| | | | |
| | | | |

---

## Appendix A: Glossary

| Term | Definition |
|---|---|
| BRD | Business Requirements Document |
| ETD | Estimated Time of Departure |
| STD | Scheduled Time of Departure |
| ATD | Actual Time of Departure |
| IATA | International Air Transport Association |
| SPA | Single-Page Application |
| CRUD | Create, Read, Update, Delete |
| DTO | Data Transfer Object |
| UUID | Universally Unique Identifier |
| ATC | Air Traffic Control |
| KPI | Key Performance Indicator |
| FIDS | Flight Information Display System |
| RBAC | Role-Based Access Control |
| CDN | Content Delivery Network |
| NFR | Non-Functional Requirement |
| FR | Functional Requirement |

---

## Appendix B: Analysis Models

**Unresolved / Not Generated** — this pipeline did not invoke a diagramming tool.
The following diagrams are recommended but not produced:
- Context diagram (actors, system boundary, external systems)
- State machine diagram for FlightStatus transitions
- Swim-lane diagram for ETD Update (BP-02)
- Data flow diagram for Statistics computation

---

## Appendix C: Issues List

All **Assumed** and **Unresolved** items consolidated from the document.

| ID | Location | Issue | Confidence |
|---|---|---|---|
| OQ-001 | §2.5, §2.7 | What user roles exist and what RBAC rules apply? | Unresolved |
| OQ-002 | §3.1.2 | Is DIVERTED status reachable via any API action? No code path sets it. | Unresolved |
| OQ-003 | §5.1 | Are there any SLA targets (latency, throughput) beyond 15 s polling? | Unresolved |
| OQ-004 | §2.7 | Should data persist across restarts? | Assumed (no) |
| OQ-005 | §2.8 | Is the DFW→LHR international route intentional scope or demo artefact? | Assumed (demo) |
| OQ-006 | §3.5 | Should duplicate detection cover same flight number on different dates, not just same time? | Unresolved |
| OQ-007 | §3.3, §3.4 | What should happen if an operator tries to depart or cancel an already-departed/cancelled flight? | Unresolved |
| OQ-008 | §4.3 | Will the deployment environment have internet access for CDN assets? | Assumed (yes) |
| OQ-009 | §7 | Are there quantitative success targets (error rates, uptime SLAs)? | Unresolved |
| OQ-010 | §2.5 | Should different user roles have different write permissions? | Unresolved |

---

## Appendix D: Screenshots / Mock-Ups

**Unresolved / Not Generated** — no UI screenshots were captured during this
automated discovery run. Screenshots would need to be taken by running the
application at `http://localhost:8080`.

---

## Appendix E: Test Scenarios

Mapped directly to test methods found during discovery.

**Source:** `src/test/java/com/airlines/etd/FlightServiceTest.java` — **Confirmed**

| Test ID | Method | What Is Tested | FR Coverage |
|---|---|---|---|
| TS-001 | `createFlightAssignsIdAndDefaults` | New flight gets UUID, ETD=STD, delayMinutes=0 | FR-001, FR-005, FR-006 |
| TS-002 | `updateEtdMarksFlightAsDelayed` | ETD update: 45 min delay → isDelayed=true, reason stored | FR-010, FR-013, FR-014 |

**Coverage Gaps (Inferred):**
- No test for duplicate flight rejection (FR-004)
- No test for ETD-in-the-past validation (FR-011)
- No test for status computation lifecycle (FR-017, FR-018, FR-019)
- No test for cancel / depart operations (FR-020, FR-021)
- No test for statistics computation (FR-022)
- No integration / controller tests

---

## Appendix F: Traceability Matrix

See `07_traceability.md` for the full matrix. Summary:

- 28 Functional Requirements identified
- 26 of 28 traced to specific source file + method/class
- 2 items (FR-009 delete guard, FR-022 statistics accuracy) have partial traces
- All 8 Non-Functional Requirements confirmed from configuration/code
